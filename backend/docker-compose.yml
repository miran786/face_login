version: '3.8'

services:
  backend:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./db:/app/db  # Persist SQLite data
    environment:
      - NODE_ENV=production
      - FRONTEND_URL=http://localhost:5173
    restart: unless-stopped
