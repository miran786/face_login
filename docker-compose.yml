
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/face_db:/app/face_db

  frontend:
    build: .
    ports:
      - "5173:5173"
    depends_on:
      - backend
